name: astrolabe
description: "Instruments for magick — a lite companion to shrutivtuber.com"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  # Swiss Ephemeris on the device. The same binding astropractise uses, and
  # the reason the astrology works with no signal and puts no load on the
  # server: a chart is arithmetic, not a request.
  sweph: ^3.2.1+2.10.3
  # Reading her writing, the schedule and the video list off shrutivtuber.com.
  http: ^1.2.2
  # Remembering a birth place, the chosen city and what has been read.
  shared_preferences: ^2.3.3
  # Zone rules, for showing a station table in a city the phone is not in.
  #
  # `flutter_timezone` — which only reports the DEVICE's zone name — is
  # deliberately absent. It still compiles Kotlin for Java 11 while every other
  # plugin here is on 17, and no combination of JVM targets satisfies both; it
  # failed the Android build outright. The device's own offset comes from
  # DateTime.toLocal() without it, and a chosen city's zone will come from the
  # site's places lookup, which already returns one.
  timezone: ^0.9.4
  # Opening the stream, the Discord and an article on the site.
  url_launcher: ^6.3.1
  # Only for an ABSOLUTE ephemeris path. See lib/services/ephemeris.dart —
  # the relative default works on desktop and fails on Android, which is the
  # worst combination.
  path_provider: ^2.1.5
  # Handing the finished sigil to the share sheet, which on a phone is the
  # only way to keep it — "copy SVG" has nowhere to be pasted on Android.
  #
  # ⚠ The file handed over is a PNG named sigil.png in a temp directory. The
  # NAME must never carry the statement: a filename travels further than the
  # file, showing up in notification shades and chat previews.
  share_plus: ^13.3.0
  # Reading .mbf packs, which are zip containers. Only used when a language is
  # actually downloaded — nothing is unpacked at startup.
  archive: ^3.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  # The station tests need the real Swiss Ephemeris, which means a real target.
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  assets:
    # ⚠ Her drawings, and they may be absent — see lib/services/art.dart.
    - assets/art/
    - assets/fonts/

  fonts:
    # Her two faces, taken from the site's own files rather than re-picked, so
    # the app is recognisably the same thing. See tool/build_fonts.py.
    - family: Commissioner
      fonts:
        - asset: assets/fonts/Commissioner-Regular.ttf
          weight: 400
        - asset: assets/fonts/Commissioner-SemiBold.ttf
          weight: 600
    - family: EBGaramond
      fonts:
        - asset: assets/fonts/EBGaramond-Medium.ttf
          weight: 500
        - asset: assets/fonts/EBGaramond-SemiBold.ttf
          weight: 600
    # Zodiac and planet glyphs. A 5.6KB cut of DejaVu Sans holding the 29
    # codepoints the wheels and tables print, and nothing else — the text
    # faces have none of them, and a missing glyph draws as a hollow box.
    - family: AstroSymbols
      fonts:
        - asset: assets/fonts/AstroSymbols.ttf

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
